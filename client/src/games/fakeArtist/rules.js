// Plain-language rules for the "How to Play" popup. See
// imposter/rules.js for the shape.
export const rules = {
  summary:
    "Everyone draws ONE small addition to a single shared picture of a secret word — except the Fake Artist, who only knows the category and has to bluff. Then you vote on who the faker is, and the faker gets one guess at the word.",
  bullets: [
    "One random player each round is the Fake Artist. Everyone else sees the secret word; the Fake Artist only sees its category (e.g. \"it's an animal\").",
    "Take turns, one at a time, each adding a bit to the SAME growing drawing. You get a short timer AND an ink limit (a meter that drains as you draw) — keep it small and let others carry it.",
    "After every turn the updated drawing is shown to everyone, so watch for the contribution that doesn't quite fit.",
    "Once everyone's drawn, vote for the Fake Artist (you can't vote yourself). Points for fingering the faker; points for the faker if they slip through.",
    "Win or lose the vote, the Fake Artist then gets ONE guess at the real word — nail it for a bonus point.",
  ],
}
