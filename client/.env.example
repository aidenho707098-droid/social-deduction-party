# Copy to .env.local for local overrides, or set this in the Vercel
# dashboard (Project -> Settings -> Environment Variables) for deploys.
#
# URL of the deployed server (no trailing slash). Leave unset for local
# dev — the app then talks to <page-hostname>:3001 automatically.
VITE_SERVER_URL=https://your-server-name.onrender.com
